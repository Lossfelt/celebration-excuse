{
  "name": "celebration-excuse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check:holidays": "node --experimental-strip-types scripts/check-holiday-data.mjs",
    "check:milestones": "node --experimental-strip-types scripts/check-personal-milestones.mjs",
    "check:sources": "node --experimental-strip-types scripts/check-holiday-sources.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^8.0.8"
  }
}
